# Core dependencies
streamlit

# Thirdweb SDK for EVM blockchain interaction
thirdweb-sdk

# PDF to image conversion for certificates
pdf2image

# Environment variable management
python-dotenv

# Web3 and Ethereum dependencies
web3
eth-account

# Install patched Parsimonious (fixes getargspec issue)
git+https://github.com/erikrose/parsimonious.git@master
